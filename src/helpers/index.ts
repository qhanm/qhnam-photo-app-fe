import {getStorage, setStorage} from "./storage.helper";
import {delayLoading, setErrorFormik} from "./func.helpers";

export const Helpers = {
    setStorage,
    getStorage,
    delayLoading,
    setErrorFormik,

}
